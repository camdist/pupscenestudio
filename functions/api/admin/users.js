import {json,requireAdmin,audit} from '../../_lib/auth.js';
export async function onRequestGet(ctx){const a=await requireAdmin(ctx);if(a.response)return a.response;const url=new URL(ctx.request.url),q=(url.searchParams.get('q')||'').trim().toLowerCase();const pattern='%'+q+'%';const rows=await ctx.env.DB.prepare(`SELECT u.id,u.email,u.name,u.role,u.status,u.created_at,u.last_login_at,e.plan,e.status entitlement_status,e.credits_remaining,e.renewal_at,e.subscription_id,(SELECT COUNT(*) FROM generation_events g WHERE g.user_id=u.id) generation_count FROM users u LEFT JOIN entitlements e ON e.user_id=u.id WHERE (?='' OR lower(u.email) LIKE ? OR lower(COALESCE(u.name,'')) LIKE ?) ORDER BY u.created_at DESC LIMIT 300`).bind(q,pattern,pattern).all();return json({ok:true,users:rows.results||[]})}
export async function onRequestPatch(ctx){const a=await requireAdmin(ctx);if(a.response)return a.response;const b=await ctx.request.json();if(!b.userId)return json({ok:false,error:'user_required'},400);const user=await ctx.env.DB.prepare('SELECT * FROM users WHERE id=?').bind(b.userId).first();if(!user)return json({ok:false,error:'not_found'},404);const status=['active','suspended','pending'].includes(b.status)?b.status:user.status;await ctx.env.DB.prepare('UPDATE users SET status=?,name=COALESCE(?,name) WHERE id=?').bind(status,b.name||null,b.userId).run();let ent=await ctx.env.DB.prepare('SELECT * FROM entitlements WHERE user_id=?').bind(b.userId).first();const plan=['none','3-storyboards','unlimited-monthly'].includes(b.plan)?b.plan:(ent?.plan||'none');const es=['inactive','active','cancelled','expired','past_due'].includes(b.entitlementStatus)?b.entitlementStatus:(ent?.status||'inactive');const credits=Math.max(0,Math.floor(Number.isFinite(Number(b.creditsRemaining))?Number(b.creditsRemaining):(ent?.credits_remaining||0)));const renewal=b.renewalAt?Number(b.renewalAt):null;const now=Date.now();await ctx.env.DB.prepare(`INSERT INTO entitlements (user_id,plan,status,credits_remaining,renewal_at,updated_at) VALUES (?,?,?,?,?,?) ON CONFLICT(user_id) DO UPDATE SET plan=excluded.plan,status=excluded.status,credits_remaining=excluded.credits_remaining,renewal_at=excluded.renewal_at,updated_at=excluded.updated_at`).bind(b.userId,plan,es,credits,renewal,now).run();await audit(ctx,a.session.user_id,'update_user',b.userId,{status,plan,entitlementStatus:es,creditsRemaining:credits,renewalAt:renewal});return json({ok:true})}
