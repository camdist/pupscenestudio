import {json,requireUser,adminEmail,normEmail} from '../../_lib/auth.js';
export async function onRequestPost(ctx){const r=await requireUser(ctx);if(r.response)return r.response;const s=r.session,now=Date.now();if(normEmail(s.email)===adminEmail(ctx.env))return json({ok:true,plan:'unlimited-monthly',unlimited:true,creditsRemaining:null});if(s.entitlement_status!=='active')return json({ok:false,error:'no_active_plan',checkoutUrl:'/checkout.html'},402);if(s.plan==='unlimited-monthly'){if(s.renewal_at&&Number(s.renewal_at)<now)return json({ok:false,error:'subscription_expired',checkoutUrl:'/checkout.html?plan=unlimited-monthly'},402);await ctx.env.DB.prepare('INSERT INTO generation_events (id,user_id,plan,created_at) VALUES (?,?,?,?)').bind(crypto.randomUUID(),s.user_id,s.plan,now).run();return json({ok:true,plan:s.plan,unlimited:true,creditsRemaining:null})}if(s.plan==='3-storyboards'){const x=await ctx.env.DB.prepare("UPDATE entitlements SET credits_remaining=credits_remaining-1,updated_at=? WHERE user_id=? AND status='active' AND credits_remaining>0").bind(now,s.user_id).run();if(!x.meta?.changes)return json({ok:false,error:'no_credits',checkoutUrl:'/checkout.html?plan=3-storyboards'},402);await ctx.env.DB.prepare('INSERT INTO generation_events (id,user_id,plan,created_at) VALUES (?,?,?,?)').bind(crypto.randomUUID(),s.user_id,s.plan,now).run();const e=await ctx.env.DB.prepare('SELECT credits_remaining FROM entitlements WHERE user_id=?').bind(s.user_id).first();return json({ok:true,plan:s.plan,creditsRemaining:Number(e?.credits_remaining||0)})}return json({ok:false,error:'no_active_plan',checkoutUrl:'/checkout.html'},402)}
