{
  "name": "pupscene-studio-cloudflare",
  "version": "13.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "db:schema:local": "wrangler d1 execute pupscene-db --local --file=./cloudflare/schema-v13.sql",
    "db:schema:remote": "wrangler d1 execute pupscene-db --remote --file=./cloudflare/schema-v13.sql",
    "db:migrate-v12-v13": "wrangler d1 execute pupscene-db --remote --file=./cloudflare/migrate-v12-to-v13.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
